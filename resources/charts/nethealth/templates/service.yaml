apiVersion: v1
kind: Service
metadata:
  name: {{ include "nethealth.fullname" . }}
  labels:
    {{- include "nethealth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "nethealth.selectorLabels" . | nindent 4 }}
